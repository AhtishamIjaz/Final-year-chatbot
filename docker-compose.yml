version: '3.8'

services:
  medgpt:
    build: .
    image: medgpt:latest
    container_name: medgpt-app
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    env_file:
      - ./backend/.env
    restart: unless-stopped
